{% from "service_base.yml.j2" import standard_service with context %}
{{ standard_service(
    name="flaresolverr",
    internal_port=8191,
    env=[
        'LOG_LEVEL=info',
        'LOG_HTML=false',
        'CAPTCHA_SOLVER=none'
    ],
    healthcheck_cmd=["CMD", "python3", "-c", "import http.client; conn = http.client.HTTPConnection('localhost', 8191); conn.request('GET', '/'); resp = conn.getresponse(); exit(0 if resp.status == 200 else 1)"]
) }}
