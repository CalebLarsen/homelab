---
- name: Template Sonarr config.xml
  ansible.builtin.template:
    src: config.xml.j2
    dest: "{{ config_root }}/sonarr/config.xml"
    owner: "{{ puid }}"
    group: "{{ pgid }}"
    mode: "0644"
