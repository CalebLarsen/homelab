---
# No per-service pre-deploy configuration needed.
# settings.json is managed by Seerr itself (auto-migrated from Overseerr on
# 2026-04-26). Settings change via the UI or via api_wiring.yml POST/PUTs;
# the on-disk file is not templated. See decisions/0007.
